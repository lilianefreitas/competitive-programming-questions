#include <bits/stdc++.h>
using namespace std;

// declarando as variáveis
int a, b;

int main() {
    
    // pegando os valores e salvando nas variáveis
    scanf("%d%d", &a, &b);

    // fazendo a operação necessária
    int soma = a + b;

    // imprimindo o resultado
    // colocar barra n depois do %d
    printf("SOMA = %d", soma);

    return 0;
}
