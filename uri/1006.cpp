#include <bits/stdc++.h>
using namespace std;

double a,b,c; 

int main() {
    scanf("%lf%lf%lf", &a, &b, &c);

    double media = ((a * 2) + (b * 3) + (c * 5)) / 10;

    printf("MEDIA = %.1lf\n", media);


    return 0;
}
