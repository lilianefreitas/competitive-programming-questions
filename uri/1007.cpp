#include <bits/stdc++.h>
using namespace std;

int a, b, c, d;

int main() {
    scanf("%d%d%d%d", &a, &b, &c, &d);

    int diferenca = (a *b) - (c * d);
    
    printf("DIFERENCA = %d\n", diferenca);

    return 0;
}
